<p id="notice"><%= notice %></p>

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/background.png);">
  <div class="container">
    <h1>Peerclub.io, rentre dans le game !</h1>
  </div>
</div>
<div class="container">
    <h2><strong><%= @group.title %></strong></h2>
    <p>
    <strong>Créer par : <%= @group.user.first_name %></strong> - <%= @group_subs.count %> / <%= @group.max_attendees %> place<%unless @group.max_attendees.nil?%><% if (@group.max_attendees > 1) %>s<%end%><%end%>.
    <br>
    Début : <%= @group.starting_date %>
    <br>
    <%= @group.meeting_point %> <%= @group.city %> - <%= @group.work_period %>
  </p>
  <p>
    <strong>Description:</strong>
    <%= @group.description %>
  </p>
  <br>

 
 <%=@group_subs.count %>
 <%unless @group.max_attendees.nil?%>
  <% if (@group_subs.count < @group.max_attendees) || (@user_sub == true) %>
  <%end%> 
    
    <% if @user_sub == true %>
      <%= link_to "Leave group", {:controller => "subscriptions", :action => 'destroy', :group_id => @group.id}, class: "btn-lg btn-info", method: :delete %>
    <%end%>
    <% if @user_sub == false %>
      <%= link_to "Join group", group_subscriptions_path(@group.id), class: "btn-lg btn-info", method: :post %>
    <%end%>

  <%else%>
    <span class="btn-lg btn-info">Complet</span>
 <%unless @group.max_attendees.nil?%>

  <%end%> 
  <%end%> 
      <%= link_to "Join group", group_subscriptions_path(@group.id), class: "btn-lg btn-info", method: :post %>



  <br>
  <br>
  <p>
    <strong>Udemy course title:</strong>
    <%= @group.udemy_course_title %>
  </p>

  <p>
    <strong>Udemy course:</strong>
    <%= @group.udemy_course_id %>
  </p>

  <p>
    <strong>Udemy course picture:</strong>
    <%= @group.udemy_url_img %>
  </p>
  <p>
    <strong>Remote:</strong>
    <%= @group.remote %>
  </p>
  
<nav>
  <div class="nav nav-tabs" id="nav-tab" role="tablist">
    <a class="nav-item nav-link active" id="nav-home-tab" data-toggle="tab" href="#nav-home" role="tab" aria-controls="nav-home" aria-selected="true">Course</a>
    <a class="nav-item nav-link" id="nav-profile-tab" data-toggle="tab" href="#nav-profile" role="tab" aria-controls="nav-profile" aria-selected="false">Discussion</a>
    <a class="nav-item nav-link" id="nav-contact-tab" data-toggle="tab" href="#nav-contact" role="tab" aria-controls="nav-contact" aria-selected="false">Attendees</a>
  </div>
</nav>
<div class="tab-content" id="nav-tabContent">
  <div class="tab-pane fade show active" id="nav-home" role="tabpanel" aria-labelledby="nav-home-tab">
    <%= render 'udemy_course' %>
  </div>
  
  <div class="tab-pane fade" id="nav-profile" role="tabpanel" aria-labelledby="nav-profile-tab">
    <%= render 'comments/comment' %><br /><br />
    <%= render 'comments/form' %>
  </div>
  
  <div class="tab-pane fade" id="nav-contact" role="tabpanel" aria-labelledby="nav-contact-tab">
    <%= render 'subscriptions/group_subs' %>
  </div>
</div>


<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
</div>

<script>
$('#myTab a').on('click', function (e) {
  e.preventDefault()
  $(this).tab('show')
})

</script>
